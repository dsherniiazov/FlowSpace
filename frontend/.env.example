# ------------------------------------------------------------
# Frontend API target
# ------------------------------------------------------------
# Optional. If empty, frontend auto-uses current browser host + port 8000.
VITE_API_BASE_URL=

# ------------------------------------------------------------
# Frontend admin UI access (emails)
# ------------------------------------------------------------
VITE_ADMIN_EMAILS=admin@example.com
